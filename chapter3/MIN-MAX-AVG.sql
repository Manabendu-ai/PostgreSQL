-- Calculating MAX, MIN, AVERAGE;

SELECT MAX(price) FROM car; -- Most expensive  Car

SELECT MIN(price) FROM car; -- Cheapest  Car

SELECT AVG(price) FROM car; -- Average  Car price

SELECT ROUND(AVG(price)) FROM car; -- Rounding off the avg price;